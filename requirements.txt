# requirements.txt — minimal & practical set for this forecasting project
# NOTE: update versions if you want exact reproducibility; these are known-stable combo.

# web / api
fastapi==0.122.0
uvicorn==0.38.0

# database / db drivers
SQLAlchemy==2.0.22
psycopg[binary]==3.2.0          # PostgreSQL driver (binary wheel)

# data stack
numpy
pandas

# ML
scikit-learn
joblib==1.3.2

# plotting & reporting
matplotlib==3.8.0
jupyterlab==4.3.1

# convenience / dev
python-dotenv==1.0.0
pytest==7.4.0

# Optional / extras used by uvicorn[standard] (if not installed via system)
uvloop==0.22.1
httptools==0.7.1
watchfiles==1.1.1

# Streamlit & Dashboard
streamlit==1.28.1
plotly==5.18.0
altair==5.0.1

# MLOps & Monitoring
python-dateutil==2.8.2
requests==2.31.0

# (optional) if you plan to use XGBoost later
# xgboost==2.1.0
